import React from 'react';
import { Container, Typography } from '@mui/material';

function OrderSuccess() {
  return (
    <Container sx={{ mt: 10 }}>
      <Typography variant="h1" align="center">Sanju</Typography>
    </Container>
  );
}

export default OrderSuccess;